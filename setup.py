# setup.py
from setuptools import setup, find_packages

setup(
    name='problemas',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Diana Sanjuan',
    description='Problemas de optimización con restricciones',
)
